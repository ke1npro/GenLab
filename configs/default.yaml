output:
  dir: outputs
  temp_dir: outputs/tmp
  manifest: true

model:
  dtype: float16
  seed: null

hardware:
  enable_offload: false
  enable_amp: false

download:
  hf_transfer: auto
  verify_space: true

services:
  huggingface:
    token: null
